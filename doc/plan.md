# 作業計画と設計判断

**この文書は、Y8960Sequencer の設計判断・その前提・見送った案・未決事項・進捗を
持つ。** 読者は、このリポジトリで作業する AI と開発者。経緯はこの文書にだけ書く。

## 文書の役割

| 文書 | 役割 | 読者 |
|---|---|---|
| `README.md` | 成果物（使い方とビルド手順） | 利用者、開発者 |
| `doc/plan.md`（この文書） | 情報リソース（設計判断と経緯） | AI、開発者 |
| `doc/rom-feedback.md` | 情報リソース（ROM へ返す相違点と不足情報、プレイヤーの暫定の扱い） | ROM の開発者、AI |

`doc/` の文書は配布物に入れない（利用者の決定、2026-09-19）。README からも案内しない。
リリースに付くソースアーカイブからは `.gitattributes` の `export-ignore` で外している
ので、AI 向けの文書を足したらそこにも足す。

## 目的と範囲

Y8960 BASIC Extension の `MSAVE` が書き出すシーケンスデータ（`"Y8SQ"` ブロック、
`Y8960BasicExtension/doc/bytecode.md`）をファイルから読み、音源チップ
エミュレータで鳴らす**スタンドアローンのプレイヤー**。

- Windows / Linux / macOS
- コマンドラインと GUI
- **再生だけを提供する。** MML のコンパイル、BASIC との連携、MSX エミュレータ
  への組み込みは範囲外

## 決定事項

### 再生部は ROM の再生経路を C++ に移植する

ROM のシーケンサとデバイスドライバ（`Y8960BasicExtension/src/resident/seq*.asm`、
`src/dev/*.asm`、音律表と音色表）が各チップに書くレジスタの列を、C++ で再現して
エミュレータに渡す。

- 移植の元にした ROM の版を、移植したコードの側に記録する。ROM は開発中で、
  再生経路も動く
- **移植はコミット済みの版から読む**（`git show <commit>:<path>`）。ROM の作業ツリーは
  別のセッションが編集していて、未コミットの途中の状態を写すことがある
- シーケンサとドライバの移植は、`doc/rom-feedback.md` の A の修正が ROM に
  コミットされてから行う。それまでは ROM の中身に依らない部分（ブロックの読み込み、
  エミュレータの読み込み、音声出力と tick のタイミング、CLI）を作る

### ROM のコードと仕様が食い違うときは、仕様に合わせる

- 利用者の方針（2026-09-15）。ROM 側は不具合として直す
- 仕様が黙っている箇所と、スタンドアローンで足りない情報は、ROM 側へ返して仕様を
  調整する。返すものは `doc/rom-feedback.md` に溜め、結論が出るまでは同文書の
  「プレイヤーの暫定」で作る
- **前提**: 仕様文書（`bytecode.md` / `basic-reference.md`）が ROM の意図を表して
  いること。仕様だけでは決まらないものは同文書で ROM 側に問う

### ADPCM のサンプルは、シーケンスとは別のファイルで受け取る

- 利用者の方針（2026-09-15）。Y8960 の ADPCM は Y8950 と互換なので、adpcm_packer の
  `adpcm-b` の出力を使う
- ボイスファイル番号とサンプルの位置・速さを結び付ける情報（adpcm_packer の
  `.json` に当たるもの）は、**シーケンスデータのチャンクとして入る見込み**
  （利用者、2026-09-15）。チャンクの形は ROM 側で確定したら利用者から伝えられる
- **`--adpcm` が受け取るのは `Y8PC` 形式だけ**（利用者の決定、2026-09-17）。
  `CALL EXPORT PCM` が書き出す形で、仕様は ROM の `doc/pcmfile.md`。生のダンプは
  受け取らない
- ボイスファイルの設定は、ブロックのチャンク `03` にも入る。**両方あるときは
  `Y8PC` を優先する**（利用者の決定、2026-09-17）。`Y8PC` の設定は「全部置き換える」
  ものなので、混ぜない
- 結び付けが無いボイスファイルのキーオンは、ROM で `SET PCM` をしていないときと
  同じく何もしない

### 3層に分ける

| 層 | 中身 | 依存してよいもの |
|---|---|---|
| コア | ブロックの読み込み、シーケンサ、デバイスドライバ、エミュレータの呼び出し | エミュレータの C API だけ |
| CLI | 引数の解釈、音声出力 | コア |
| GUI | 画面 | コア |

- **前提**: GUI のツールキットを替えても、作り直すのは GUI 層だけで済むこと

### ビルドは CMake、GUI は Dear ImGui + SDL3

- 利用者の選択（2026-09-15）
- 依存は CMake の FetchContent で取る

### エミュレータは、利用者が置いたビルド済みの共有ライブラリを実行時に読み込む

- 利用者の選択（2026-09-15）
- 使う3本と担当するブロック

| ライブラリ | チップ名 | Y8960 のブロック |
|---|---|---|
| Y8960emu | `OPLLEX` ×2、`OPL2EX` ×2 | OPLL-EX、OPL2-EX（ADPCM を含む） |
| EPSGemuEngine | `SSGS` | SSGS |
| DSAemuEngine | `DCSG` ×2、`SCC` | DCSG、SCC |

- 3本とも同じ名前の関数（`FmEngine_*`、`FmEngineApi.h`）を公開しているので、
  1つの実行ファイルに静的にリンクすると名前が衝突する。**実行時に読み込む
  方式はこの点でも必要**
- **実行ファイルと同じフォルダから探す**（利用者の決定、2026-09-15）。探す名前は
  各ライブラリの CMake が出力する名前

| ライブラリ | Windows | Linux | macOS |
|---|---|---|---|
| Y8960emu | `Y8960emuEngine.dll` | `libY8960emuEngine.so` | `libY8960emuEngine.dylib` |
| EPSGemuEngine | `EPSGemuEngine.dll` | `libEPSGemuEngine.so` | `libEPSGemuEngine.dylib` |
| DSAemuEngine | `DSAemuEngine.dll` | `libDSAemuEngine.so` | `libDSAemuEngine.dylib` |

- **前提**: 3本が FmEngineApi の C API を保ち、出力名を変えないこと。変わったら
  上の表と、名前を持つコード1箇所を直す

### コマンド名は `y8960player`、第1引数がシーケンスファイル、`--adpcm` で ADPCM

```
y8960player <シーケンスファイル> [--adpcm <ADPCM サンプルファイル>] [--tick <0-2>]
            [--repeat <0-255>] [--mute <指定>]... [--wav <出力ファイル>]
```

- 利用者の決定（2026-09-15、`--tick` は 2026-09-18、`--mute` `--wav` `--repeat` は 2026-09-19）
- **`--tick` の値の意味は `CALL MINIT` の分解能と同じ。** 0 が約60Hz、1 が約100Hz、
  2 が約200Hz、省略時は 2。ROM に無い 50Hz は CLI からは選べない（GUI にはある）
- **`--mixer` は保留。** デバイスごとの音量を `MIXER` と同じ意味で指定したいが、
  ROM の `MIXER` 文がまだ実装されておらず、実機のデジタルミキサーのレジスタ仕様も
  決まっていない（利用者、2026-09-18）。値の口だけは `Y8960Chips::setGain` に
  用意してあり、`tests/render_test.cpp` が効くことを見ている
- **`--repeat` の値の意味は `CALL MSTART` のリピート回数と同じ**（0-255、0 は無限、
  省略時 1）。利用者の指示（2026-09-19）が `--repeat 0` と書いていたので、それに
  合わせて足した

### コンソールと画面に出す文字列は英語で、ASCII に収める

- 利用者の決定（2026-09-19）。CLI のメッセージ、コアのエラー文（CLI と GUI の両方に
  出る）、GUI の文字が対象。コメントは日本語のまま
- **`tools/check_ascii_strings.py` が `src/` の文字列リテラルを検査し、ctest の
  `ascii_strings` として走る。** 非 ASCII の文字列リテラルがあれば場所を出して落ちる
- 利用者が渡したファイル名はそのまま表示するので、そこには非 ASCII が出うる。
  Windows でそれが化けないよう、CLI はコンソールの出力を UTF-8 にしている

### WAV の書き出し（CLI の `--wav`）

- 利用者の指示（2026-09-19）。**`--repeat 0` と一緒に指定したらエラー**（利用者の決定）
- **書き出しの形はこちらで決めた**：48000Hz・16bit・ステレオの PCM。再生と同じ
  サンプリング周波数。変えるなら `src/cli/main.cpp` の定数と `WavWriter` だけ
- 音声デバイスは開かず、鳴らさない（`PlaybackEngine::open` の `withAudio`）
- **曲が終わったあとの余韻**：音が 0.5 秒続けて消えるまで、最長 5 秒書く。
  **16bit の 1 LSB までは無音とみなす** ―― DSAemuEngine（`0f5c786`）の SCC は、
  キーオフのあとも 1 LSB ほどの一定値を出し続けていて（測った）、厳密な 0 を
  待つと毎回 5 秒まで伸びた
- 終わらない曲の保険として 30 分で打ち切り、その旨を出す。`!` の組み合わせや
  ダルセーニョで、`--repeat` が 1 でも終わらない曲はありうる

### ミュート（CLI の `--mute`、GUI のチップ・チャンネル単位）

- 利用者の決定（2026-09-19）。書式は
  `--mute {<チップシンボル>[,<CH番号>]|T<トラック番号>|<トラックシンボルA-P>}`、
  頭の `!` で「それ以外」、複数指定可、使われていないものの指定は受け付けて何もしない
- **チップシンボルはこちらで決めた**：`SSGS` `OPLLEX1` `OPLLEX2` `OPL2EX1` `OPL2EX2`
  `DCSG1` `DCSG2` `SCC`。`bytecode.md` のデバイス表の名前から空白を除いたもの
  （ROM の `naming.md` はチップの名前を持たない）。**変えるなら `src/core/mute.cpp`
  の表1つと README の表**
- 大文字と小文字は区別しない。CH 番号はシーケンスデータのチャンネル番号と同じ
- **`!` の付いた指定と付かない指定が混ざったとき**：`!` の付いた指定の和集合に
  入らないトラックを黙らせ、そのうえで `!` の無い指定を黙らせる。**利用者の指定に
  この場合の規則は無く、こちらで決めた**
- 形は合っていてもチップが持たないチャンネル（`SCC,7` など）は、打ち間違いとして
  拒む。使われていないだけのもの（`DCSG2` を使わない曲の `DCSG2`）は受け付ける
- **どの指定もトラックに解決する。** 1チャンネルを持つのは1トラックだけなので、
  チャンネルはそれを持つトラック、チップはそのチップのトラック全部になる
- トラックのミュートは ROM の `MMUTE`（シーケンス単位）と同じ動き ―― キーオンと
  リズムの打撃を止め、音量は 0 を書く。鳴っている音はキーオフしない
- GUI ではリズムチャンネルを1本として黙らせる（楽器ごとには分けない）。CLI の
  チャンネル指定と揃えるため

### GUI のレベルメーターは、ブロックごと・チャンネルごとに分ける

- 利用者の決定（2026-09-18）。見た目は `../FITOM_X` の `LevelMeterPanel` に合わせる
  ―― LED のセグメントを積んだバー、下が緑・上が黄と赤、消えているセグメントも
  暗い色、ピークホールド、バーの下にラベル。ブロックを1つの帯として縦に積む
- **エミュレータから取れるのはチップ単位の音だけなので、チャンネルごとの値は
  FITOM_X と同じくキーオンとそのときの音量から作る。** シーケンサが
  `ChannelActivity`（`src/core/activity.h`）に書き、画面が読む。落ち方は画面が作る
- リズムチャンネルは楽器5つ（BD・SD・TM・CY・HH）に分ける。`V` と `@A` の
  どちらが効いたかは、そのときのアクセントのビットで決まる
- 既定では、読み込んだシーケンスが使うチャンネルだけを出す。`All channels` を
  立てると8ブロックの全チャンネルを出す
- ブロックごとの音量（`--mixer`）のために、**音源ブロックごとにエンジンを1つ持つ
  形に変えた**（前は3本のライブラリに1つずつ）。こちらは実際の出力の最大振幅を
  持つので、ブロック単位のレベルは音そのものから取れる
- **前提**: ブロックごとに別のエンジンを持っても、合成した音は1つのエンジンに
  まとめたときと変わらない。エンジンはチップごとに生成して足しているため

### Windows 版の配布物は、エミュレータを同梱した zip

利用者の決定（2026-09-19、v0.1.0）。`tools/package_windows.py` が作る。

- 3つのエミュレータの DLL を同梱し、`licenses/` に同梱物すべてのライセンス文を
  入れる（エミュレータ3つ、ymfm、MAME 由来の3つ、emu* の5つ、SDL3、Dear ImGui）。
  どれも表示を条件に再頒布を許すライセンス。エミュレータの `extern/` が増えたら、
  スクリプトの一覧も足す
- CRT を静的リンクする（`CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded`）。VC++ の
  再頒布パッケージを利用者に入れさせないため。モジュールごとに CRT のヒープが
  別になるが、FmEngineApi は C の関数だけで、確保と解放がモジュールをまたがない
  ので成り立つ。**API がモジュールをまたいでメモリを渡すようになったら成り立たない**
- `subst` で割り当てたドライブの上でビルドする。SDL3 の `__FILE__` が実行ファイルに
  残り、実際のパスには利用者の名前が入るため。スクリプトは仕上がったバイナリを
  ホームフォルダのパスで検査し、見つかれば zip を作らない
- Linux と macOS の配布物は作らない（ソースからビルドする）

### 入力ファイルは名前で選ばず、中身のヘッダで判断する

- 利用者の決定（2026-09-15）。拡張子などファイル名には制限を設けない
- 先頭の `"Y8SQ"` ヘッダ（`bytecode.md`「ブロック」）を見て受け付ける
- MSX の `BSAVE` で保存したファイルは先頭に 7 バイトの見出し（`FEh` で始まる）が
  付く。**付く場合と付かない場合があるので自動で判別する**（利用者の決定、
  2026-09-15）

### ライセンスは MIT、著者は madscient

- 利用者の決定（2026-09-15）。`LICENSE` に置いた
- **前提**: 音色データはシーケンスに埋め込まれていて、プレイヤーは ROM の音色表を
  持たない（利用者の理解、2026-09-15）
- ROM の `35cb6cb` で、OPL2EX のリズム音色（音色の索引 32-34）と ADPCM のボイス
  ファイルの控え（チャンク `03`）がブロックに入るようになった
- ROM の `b91892a` で、`@` を書かないトラックにも既定の音色のレコードが入るように
  なった。**これでブロックは音色について閉じ、プレイヤーは ROM の音色表を持たない**

## 見送った案

### ROM の Z80 コードをエミュレートして鳴らす

ROM イメージをそのまま Z80 エミュレータで走らせ、I/O ポートへの書き込みを
エミュレータに渡す案。ROM と同じ音になることが構造上保証される。

見送った理由:
- カートリッジのマッパー、RAM バンク、割り込み、MSX-TIMER を模す必要があり、
  MSX エミュレータの一部を作ることになる
- ROM イメージの配布が要る。ROM には他の拡張BASIC と、出典の違う音色データが
  含まれる
- 利用者から、MSX エミュレータへの統合は不要で、スタンドアローンの再生だけを
  提供すると示された

### エミュレータを submodule にしてこのリポジトリで一緒にビルドする

版を gitlink で固定でき、1回のビルドで揃う案。利用者が DLL を置く方式を選んだため
見送った。

### Debian 11（WSL2）で窓と音声出力を確かめる

見送った。理由：その環境には SDL3 が求める開発用パッケージをもう入れられない
（利用者の判断、2026-09-19）。ビルド・試験・WAV の書き出しはその環境で確かめてある。
パッケージを入れられる Linux が用意できれば、この判断は要らなくなる。

## 未決事項

### ROM 側で結論を待っているもの

`doc/rom-feedback.md` の「決めてほしいこと」。プレイヤーは各項目の暫定で作り、
暫定の箇所はコードからその項目を辿れるようにしておく。

### 外から見える値（まだ決めていない）

- MSX2 BASIC の `COPY 配列 TO ファイル` が書く形に見出しが付くかは**未確認**。
  付くなら、その形も受け付けるかを決める
- ブロックの外にある入力のうち、`MTUNE` の与え方。一覧は `doc/rom-feedback.md` の D

### まだ動かしていない環境

- **Linux の窓と音声出力（未検証）。** Debian 11（WSL2、gcc 10.2）では、SDL3 が求める
  X11/Wayland と音声の開発用パッケージが入っておらず、`SDL_UNIX_CONSOLE_BUILD=ON` で
  ビルドした。GUI はコンパイルとリンクまでで、起動していない。この環境での確認は
  見送った（下の「見送った案」）。確かめるには、パッケージを入れられる別の Linux が要る
- **macOS（未検証）。** ビルドも実行もしていない。`src/core/platform.cpp` の分岐
  （`_NSGetExecutablePath`、`dlopen`）とライブラリ名 `lib*.dylib` を書いてあるだけ

### ブロック間の音量比

実機の Digital Mixer のレジスタ仕様は見つかっていない。openMSX_Y8960 の設定値は
参考になるが、実機の資料ではない。

## 進捗

- 2026-09-25: **OPL2EX のリセットで WSE（01h の bit5）を立てるようにした。** Y8960emu
  `da2ab34`（ymfm 81aec25）から、波形選択は YM3812 と同じく WSE が無いと効かない。
  ROM も WSE を立てないので、そのままでは波形選択がすべてサインで鳴る。利用者の決定で、
  プレイヤーが先に立て、ROM へは `doc/rom-feedback.md` の C3 で返す
  - `tests/render_test.cpp` に、半波サインのキャリアで負の側が出ないことを見る検査を
    足した。`da2ab34` で WSE を立てないと落ち（最小 -0.0116）、立てると通る（-0.0005）。
    `e27862a` ではどちらでも通る
  - **前提**: 拡張 OPL2 部の波形選択が YM3812 と同じく WSE に従うこと。ROM が C3 を
    取り込んだら、この書き込みは ROM と同じ場所に揃える
- 2026-09-25: **再生経路を ROM の `b7ff718` まで突き合わせた。** 移植元の `d2e64a3` から後の
  ROM の `src` のコミットを1件ずつ見て、挙動が変わるものだけを写した
  - `337eec6`（写した）: `(DC)` と `(FINE)` が周を終わらせるときにキーオフしない。
    次の周の最初の音符か休符がキーを切るので、`0E` で始まるトラックだけが前の周の
    最後の音を保つ。繰り返しを使い切ったときは従来どおり全部キーオフする。
    `tests/sequencer_test.cpp` に2トラックの試験を足し、旧い挙動に戻すと `0E` の
    トラックの音が 48 tick 目で切れて落ちることを確かめた
  - `b9d83c1`（写さない）: 巻き戻しで既定の音色を書くのを SSGS・DCSG・ADPCM チャンネル
    だけにした。こちらでは FM と SCC のドライバが番号 0 の `setVoice` をもともと捨てて
    いる（B1）ので、書き込むレジスタは変わらない。**前提**: FM と SCC の `setVoice` が
    既定の音色を捨て続けること
  - `10cc834`（写さない）: コンパイラ側。新しい `86`（セーニョの目印）は、`event` の
    1バイトの範囲（`80`-`BF`）で読み飛ばされる。上の `(DC)` の試験の先頭に置いて確かめた
  - `e094f4c`・`990707c`・`99fd6b8`（写さない）: ROM の実行時間だけの変更。`e094f4c` の
    MULVOL は ROM 側で新旧が 16384 通りすべてで等しいことを確かめてある（ROM のコミット
    メッセージより。こちらでは突き合わせていない）
  - `106371b`・`0046f13` は既に反映済み（下の 2026-09-18・2026-09-19）
- 2026-09-25: **Q が 8 未満で `&` と数値の無い `~` が効かなかった**のを、ROM の `b7ff718` に
  合わせて直した。利用者が YSENDING（Q7 のトリルを `&` でつないだもの）で気づいた。
  ROM も同じく鳴らし直しており、ROM 側が先に演奏側だけを直した（バイトコードと
  コンパイラは変わらない）。音符を始めるとき直後の1イベントを覗き、`45` か `D2 00 80`
  ならゲートを置かない（ROM の `TRKJOINED`）。`Q` はつないだ最後の音符にだけ、その
  音符自身の長さに対して効く
  - `tests/sequencer_test.cpp` に、Q4 での `c&e`・`c~e`（キーオン1回、e が自分の Q4 で
    切れる）と `c~100e`・`c V & e`（キーオン2回、c が Q4 で切れる）を足した。判定を
    外すと前の2つが落ち、後の2つは前後で変わらないことを確かめた
  - YSENDING.SQ を鳴らして聴く確認はしていない（未検証）
  - **前提**: コンパイラが `&` と `~` を音符の直後に出すこと。ROM がこの判定を
    「直後の1イベント」から広げたら、`Sequencer::joined` も合わせる
- 2026-09-20: **v0.1.2 をリリースした**（タグは `26834c9`）。同梱の Y8960emu を `e27862a` に
  更新した（ほかの2つは v0.1.0 と同じ）。展開した zip で、採譜作業のデータのリズムの欠けが
  0 か所であることを確かめた
- 2026-09-20: 同じ tick の KEY OFF → KEY ON が多いと、書き込み順で後ろのものの KEY OFF が
  消える件が、Y8960emu `e27862a` で直った。エミュレータは重なりごとに約 2ms を先に作るが、
  `9c687c8` はその回に求められた量の中でしか作れず、200Hz の間隔（240 サンプル）では
  重なり3つで尽きていた。`e27862a` は尽きたら書き込みを次の呼び出しへ持ち越す
  （重なり1つにつき最大約 2ms 遅れる）
  - 採譜作業のデータ（OPLLEX1 のメロディ5本＋リズム）で、リズムの打撃の欠けが
    29 か所 → 0 か所（「全部 − リズムだけミュート」と「リズムだけ」を8分音符ごとに比べた）
  - `tests/render_test.cpp` に、メロディ5本とリズムが同じ tick で鳴る試験を足した。
    `9c687c8` では打撃8回のうち1回、`e27862a` では8回とも聞こえる
- 2026-09-19: **v0.1.1 をリリースした**（タグは `8cd774b`）。KEY OFF が消える件の修正。
  同梱したエミュレータは v0.1.0 と同じ
- 2026-09-19: **同じ tick の KEY OFF → KEY ON で KEY OFF が消える**のを直した。
  Y8960emu `9c687c8` は、1回の `generate()` の中で同じチャンネルのキー状態が2度
  変わると、間に約 2ms を作って KEY OFF を見せる。ところが `Player` は呼び出し側が
  求める量で生成を区切っていたので、割り込みの直後で区切れると間を作れなかった。
  WAV の書き出し（1024 サンプルずつ）では3つの境目のうち1つ、1〜7 サンプルずつでは
  全部が消えた（`tests/render_test.cpp` の最後の検査で確かめた。直す前に落ちる）。
  リアルタイム再生では音声出力が求める量が負荷で揺れるので、消え方が負荷に依存して
  いた（**推測**：求める量が揺れることは SDL3 の仕様から。負荷をかけて聴いてはいない）。
  `Player` が割り込みの間隔1回ぶんをまとめて作ってためる形にした。
  **前提**：割り込みの間隔（200Hz で 5ms）がエミュレータの先行生成（約 2ms）より
  長いこと。ブロックの音量やミュートの効きは最大1間隔遅れる
- 2026-09-19: **v0.1.0 をリリースした**（タグは `980ff3e`）。Windows 版の zip を添付。
  同梱したエミュレータは Y8960emu `9c687c8`、EPSGemuEngine `63c8834`、
  DSAemuEngine `0f5c786`。展開した zip で CLI が動き、WAV が開発中のビルドと
  バイト単位で一致すること、5つのバイナリが VC++ のランタイムに依存しないこと
  （dumpbin）、GitHub のソースアーカイブに `doc/` の文書が入らないことを確かめた
- 2026-09-19: **Linux でビルドと試験を通した。** Debian 11（WSL2、gcc 10.2、
  CMake 3.31）で、3つのエミュレータを共有ライブラリとしてビルドし、このリポジトリを
  GUI を含めて警告なしでビルドした。試験10本が通る。CLI の `--wav` の出力は Windows の
  出力とバイト単位で一致した（ADPCM を含む曲で比べた）。窓と音声出力は未確認
  （未決事項の「まだ動かしていない環境」）
- 2026-09-19: **ADPCM がサンプルメモリを読んでいなかった**のを直した。キーオンで
  07h に START（`80h`）だけを書いていたため、エミュレータは CPU ポートの値を復号し、
  出力が一定値に張り付いていた。MEMORY_DATA（bit5）を足して `A0h` にした。ROM も同じ
  書き方なので `doc/rom-feedback.md` の C2 に挙げた
  - それまでの `tests/render_test.cpp` は「ADPCM で音が出ること」しか見ておらず、
    メモリを読まない一定値の出力でも通っていた。サンプルメモリの中身を2通りに変えて
    出力が変わることを見る検査を足した。修正前に落ち（差 0.00000）、修正後に通る
    （差 0.064）ことを確かめた
  - `y8mmlc` の出力（`.SQ` と `Y8PC`）を `--adpcm` 付きで WAV に書き出し、波形が
    出ることを確かめた。`--adpcm` の有無で結果が変わる
- 2026-09-15: 情報リソースの調査、ROM と仕様の相違点の一覧を作成。相違点は仕様に
  合わせる方針が決まり、一覧を ROM へのフィードバックの文書に改めた。ADPCM は
  adpcm_packer の出力を別ファイルで受け取る方針が決まった
- 2026-09-15: ROM の中身に依らない骨組みを作った
  - ブロックの読み込み（BSAVE 見出しの自動判別、`bytecode.md` の検査、リズムモードの
    推定）。`tests/block_test.cpp` が通る
  - エミュレータの共有ライブラリの読み込みと、8ブロックへの振り分け。
    `tests/chips_test.cpp` が8ブロックすべてにレジスタを直接書いて音が出ることを見る。
    確かめた版は Y8960emu `b695a31`、EPSGemuEngine `63c8834`、DSAemuEngine `0f5c786`
  - 割り込みの周期と TICKINC（`timer.asm`）。`tests/timing_test.cpp` が通る
  - 割り込みの時刻でサンプルを区切るレンダリングの枠（`Player`）。シーケンサが無いので
    まだ動かしていない
  - CLI は読み込み・検査・エミュレータを開くところまで。再生部が無い旨を出して
    終了コード 2 で終わる
- 音声出力（SDL3）はシーケンサを入れるときに足す。どこからも呼ばれないうちは
  動かしたことにならないため
- 2026-09-16: ROM が `4459a71` まで進み、`doc/rom-feedback.md` の食い違い10件と
  リズムモードの決め方が直った。ブロックの読み込みを新しい仕様に合わせた ――
  チャンク `03`（ADPCM のボイスファイルの控え）と、音色の索引 32-34（OPL2EX の
  リズム音色）。`tests/block_test.cpp` に両方の検査を足し、3本とも通る
- 2026-09-17: `--adpcm` の `Y8PC` 形式を読む部分（`src/core/pcmfile.cpp`）と、
  チャンク `03` との優先順位を入れた。`tests/pcmfile_test.cpp` を足し、試験4本とも通る。
  CLI で、チャンク `03` だけのブロック・`Y8PC` を渡した場合・`Y8PC` でないファイルを
  渡した場合を確認した
- 2026-09-19: CLI に `--wav`（WAV の書き出し）と `--repeat` を足した。`tests/wav_test.cpp`
  が見出しと切り詰めを見る。`--repeat 0` と `--wav` の組み合わせは断る
- 2026-09-19: ミュートを入れた（`src/core/mute.cpp`、`tests/mute_test.cpp`）。
  あわせて、周の境目で次の周を同じ tick に走らせる前に「最初の tick」の印を
  下ろしていたのを直した ―― ROM（`PASSAGAIN`）は印を立てたまま走らせるので、
  時間を取らない周は次の tick を待つ。これまでの実装だと、時間を取らない周が続くと
  1つの tick の中で回り続けうる（**コード読解で見つけた。試験では再現していない**）
- 2026-09-19: ROM の `b38acbe` で、`pcmfile.md` の件数の書き漏らし（前回の C1）が直った
- 2026-09-18: ROM の `106371b` で ADPCM のボイスファイルが 0-31 から 0-63 に
  広がったので追従した。`pcmfile.md` に 32 個だったころの数が2箇所残っていたので、
  `doc/rom-feedback.md` の C1 に挙げた
- 2026-09-18: ADPCM チャンネルと GUI を入れた。音声出力と再生の制御は
  `src/app/engine.cpp` にまとめ、CLI と GUI が共有する
  - **GUI の文字は英語**（利用者の決定、2026-09-18）。Dear ImGui の既定のフォントが
    日本語の字を持たないため。日本語にするならフォントを同梱することになる
    （`../FITOM_X` は `NotoSansJP-Regular.ttf` を同梱している）
  - GUI は開く・鳴らす・止める、繰り返し回数、tick の周期、トラックの一覧を持つ。
    ファイルを落として開ける。引数でも開ける
  - 終了の順序に注意が要る ―― `SDL_Quit()` のあとで音声ストリームを壊すと
    ヒープが壊れる。エンジンを先に畳む（**終了コードで見つけた**）
- 2026-09-18: **シーケンサとドライバを移植し、音が出るようになった。** 移植元は
  ROM の `d2e64a3`
  - 音律表は ROM のデータを写さず、`tools/zbuild/freqtab.py` と同じ式で作る。
    `tests/freqtab_test.cpp` が ROM の `freqdat.asm` と 2304 項目すべてを突き合わせ、
    差は 0 件だった（環境変数 `Y8960_FREQDAT` を渡したときだけ走る）
  - シーケンサ（`src/core/sequencer.cpp`）はイベント・ループ・ジャンプ・
    ポルタメント・クオンタイズ・テンポ・繰り返しを持つ。ドライバは SSGS・
    OPLLEX・OPL2EX（リズムを含む）・DCSG・SCC の5本
  - **ADPCM チャンネルはまだ鳴らさない**
  - `tests/sequencer_test.cpp` がレジスタ書き込みを記録するバスで動きを見る。
    `tests/render_test.cpp` が5音源のブロックを実際のエミュレータで鳴らし、
    演奏中だけ音が出ることを見る
  - CLI は SDL3 で音を出す。tick の周期は 200Hz、繰り返しは1回に固定
- 2026-09-18: ROM が `d2e64a3` まで進み、**フィードバックの項目はすべて片付いた**。
  仕様が定まったチャンク `03` の扱い（ROM は検査しない）に合わせ、壊れた項目だけを
  捨てて警告を返す形に変えた。ブロック全体を拒むと、MSX で鳴るものが鳴らなくなる
